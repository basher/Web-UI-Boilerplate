/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'all',
};

export default config;
