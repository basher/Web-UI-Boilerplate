import { Meta } from '@storybook/addon-docs/blocks';
import { DesignTokenDocBlock } from 'storybook-design-token';

<Meta title="Foundations/Other Design Tokens" />

# Other design tokens

## Spacing
- Spacing tokens use the [Utopia fluid space calculator](https://utopia.fyi/) to output the **minimum**, **maximum** and **ideal** sizes.
- Used for `margin`, `padding` and `gap` properties.

<DesignTokenDocBlock categoryName="Spacing" viewType="table" showSearch={false} />

## Borders
<DesignTokenDocBlock categoryName="Borders" viewType="table" showSearch={false} />

## Icons
<DesignTokenDocBlock categoryName="Icons" viewType="table" showSearch={false} />

## Animation duration
<DesignTokenDocBlock categoryName="Animation" viewType="table" showSearch={false} />

## UI States
<DesignTokenDocBlock categoryName="States" viewType="table" showSearch={false} />

## Additional sizing tokens
- To set maximum values for site width or line length.

<DesignTokenDocBlock categoryName="Sizing maximum" viewType="table" showSearch={false} />
