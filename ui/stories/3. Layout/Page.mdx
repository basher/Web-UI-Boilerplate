import { Preview, Story } from '@storybook/addon-docs/blocks';

# Page layouts
## Simple

<Preview>
    <Story id="layout-page--page" />
</Preview>

## With sidebar
- Note the inclusion of an additional wrapper (i.e. the `<main>` element) around the sidebar component wrapper to allow the page's CSS Grid layout to push the footer to the bottom, whilst also preventing *vertical stretching* of the sidebar component flex items on small screens.

<Preview>
    <Story id="layout-page--page-sidebar" />
</Preview>

## With right sidebar
- Please note the source order in the HTML. The sidebar comes **after** the main content, to preserve natural tab sequence for keyboard users.

<Preview>
    <Story id="layout-page--page-sidebar-right" />
</Preview>
