import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Wrapper from './Wrapper.stories';

<Meta of={Wrapper} />

# Wrapper
- Simple wrapper layout component whose only job is to constrain width of content and apply auto margins.
- Normally, this would be used to divide content areas into _"slices"_, going down the page.
- For more complex layouts (e.g. multi-column), use one of the [grid layouts](/docs/layout-grid--docs).
- There should be **NO visual styles** applied directly to wrappers (e.g. background colour). Add extra classes (e.g. `skin-1`) to apply visual styles.

<Canvas of={Wrapper.Wrapper} />
<Controls of={Wrapper.Wrapper} />

## Override margin
- Use [stack layout](/docs/layout-stack--docs) to apply vertical `gutters` between sibling wrappers.

## Override padding
- Use [utility classes](/story/utilities-helpers-and-utilities--margin-padding).

## Applying visual styles to wrapper
- Add a [skin component](/story/components-skin--skin-wrapper) classname.
