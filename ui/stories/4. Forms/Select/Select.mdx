import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as Select from './Select.stories';

<Meta of={Select} />

# Select dropdown
- Uses the native [customizable select](https://developer.mozilla.org/en-US/docs/Learn_web_development/Extensions/Forms/Customizable_select) element.

<Canvas of={Select.Select} />

## Browser compatibility
<baseline-status featureId="customizable-select"></baseline-status>
