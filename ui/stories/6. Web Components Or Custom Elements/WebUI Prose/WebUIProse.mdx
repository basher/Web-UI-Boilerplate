import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as WebUIProse from './WebUIProse.stories';

<Meta of={WebUIProse} />

# `<webui-prose>`
- Manages prose content (e.g. from a CMS editor).
- The spacing between the individual typographic elements is handled by the [stack layout](/docs/layout-stack--docs).
- Tables have a `<div class="table-container">` programatically added via JavaScript to make them responsive.

<Canvas of={WebUIProse.WebUIProse} />
