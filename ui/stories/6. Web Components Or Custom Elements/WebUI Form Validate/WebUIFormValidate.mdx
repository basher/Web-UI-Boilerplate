import { Meta, Canvas } from '@storybook/blocks';
import * as WebUIFormValidate from './WebUIFormValidate.stories';

<Meta of={WebUIFormValidate} />

# `<webui-form-validate>`
- Client-side validation using JavaScript.

<Canvas of={WebUIFormValidate.WebUIFormValidate} />

## Additional reading
- A [better approach to form validation](https://adamsilver.io/blog/the-problem-with-live-validation-and-what-to-do-instead/) using form `submit` rather than live inline validation.
- Using the [Constraint Validation API](https://developer.mozilla.org/en-US/docs/Learn/Forms/Form_validation#validating_forms_using_javascript).
