import { Meta, Canvas } from '@storybook/blocks';
import * as Breadcrumb from './Breadcrumb.stories';

<Meta of={Breadcrumb} />

# Breadcrumb

## Accessibility considerations
- See the [ARIA APG patterns](https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/examples/breadcrumb/) example.

<Canvas of={Breadcrumb.Breadcrumb} />
