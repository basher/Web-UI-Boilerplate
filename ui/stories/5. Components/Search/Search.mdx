import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as Search from './Search.stories';

<Meta of={Search} />

# Search
- Uses the native HTML `<search>` element to wrap the `<form>`.
<Canvas of={Search.Search} />

## Browser compatibility
<baseline-status featureId="search"></baseline-status>

## Predictive search (using `<datalist>`)

### Accessibility considerations
- See a [comparison of datalist versus ARIA combobox](https://www.webaxe.org/datalist-over-aria-combobox/).
- See a detailed breakdown of [remaining accessibility issues & bugs](https://adrianroselli.com/2023/06/under-engineered-comboboxen.html).

<Canvas of={Search.SearchDatalist} />

## `<webui-predictive-search>` (with dynamic results from an API)
- See the [`<webui-predictive-search>`](/docs/web-components-or-custom-elements-webui-predictive-search--docs) Web Component.
