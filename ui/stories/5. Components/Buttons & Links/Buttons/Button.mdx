import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Button from './Button.stories';
import * as IconButton from './IconButton.stories';
import * as TextIconButton from './TextIconButton.stories';

<Meta of={Button} />

## Text button
<Canvas of={Button.Button} />
<Controls of={Button.Button} />

## Icon button
- Please refer to the [icons docs](/docs/design-system-icons--docs) for all documentation relating to:
    - `<svg>` elements.
    - Using SVG sprites.

<Canvas of={IconButton.IconButton} />
<Controls of={IconButton.IconButton} />

## Text & icon button
<Canvas of={TextIconButton.TextIconButton} />
<Controls of={TextIconButton.TextIconButton} />
