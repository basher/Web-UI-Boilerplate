import { Preview, Story } from '@storybook/addon-docs/blocks';

# Wysiwyg CMS editor "prose" content
- The spacing between the individual typographic elements is handled by the [stack layout](/story/layout-stack--stack).
- Tables have a `table-wrapper` programatically added via JavaScript to make them responsive.

<Preview>
    <Story id="components-wysiwyg--wysiwyg" />
</Preview>
