import { Meta, Canvas } from '@storybook/blocks';
import * as Banners from './Banners.stories';

<Meta of={Banners} />

# Banners
- Please see [responsive image](/docs/components-images--docs) component for a description of image markup and behaviour.
- The `banner__content` uses the [stack layout](/docs/layout-stack--docs) to manage spacing between all its children.

## Fullwidth banner with text overlay

<Canvas of={Banners.FullwidthBanner} />

## Fullwidth banner with image overlay
- e.g. Gradient overlay to make text content more readable.

<Canvas of={Banners.FullwidthBannerOverlay} />

## Banner with inline image
- Uses the [fullwidth card](/story/components-cards--fullwidth-card) HTML structure & CSS behaviour.

<Canvas of={Banners.InlineBanner} />
