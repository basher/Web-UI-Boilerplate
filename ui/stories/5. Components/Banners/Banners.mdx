import { Preview, Story } from '@storybook/addon-docs/blocks';

# Banners
- Please see [responsive image](/docs/components-images--responsive-image) component for a description of image markup and behaviour.
- The `banner__content` uses the [stack layout](/story/layout-stack--stack) to manage spacing between all its children.

## Fullwidth banner with text overlay

<Preview>
    <Story id="components-banners--fullwidth-banner" />
</Preview>

## Fullwidth banner with visual overlay
- e.g. Gradient overlay to make content stand out.

<Preview>
    <Story id="components-banners--fullwidth-banner-overlay" />
</Preview>

## Banner with inline image
- Uses the [fullwidth card](/story/components-cards--fullwidth-card) HTML structure & CSS behaviour.

<Preview>
    <Story id="components-banners--inline-banner" />
</Preview>
